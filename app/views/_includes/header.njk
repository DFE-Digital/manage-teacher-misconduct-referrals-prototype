{% if data.user %}
  {% set serviceUrl = '/referrals' %}
{% else %}
  {% set serviceUrl = '/' %}
{% endif %}

{{ govukHeader({
  homepageUrl: "#",
  serviceName: serviceName,
  serviceUrl: serviceUrl,
  navigation: [
    {
      href: "/account/sign-out",
      text: "Sign in"
    } if not data.user,
    {
      href: "/account/sign-out",
      text: "Sign out"
    } if data.user
  ]
}) }}